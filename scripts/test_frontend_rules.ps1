[CmdletBinding()]param();$ErrorActionPreference='Stop';$root=(Resolve-Path (Join-Path $PSScriptRoot '..')).Path;. (Join-Path $root 'tests/helpers/TypedStudioFixture.ps1');$work=Join-Path ([IO.Path]::GetTempPath()) ('frontend-'+[guid]::NewGuid().ToString('N'));New-Item -ItemType Directory $work|Out-Null
try{$p=New-TypedStudioFixture $work 'visible' -ProductType 'landing-page' -Surfaces @('frontend-visible') -Approved;& "$root/scripts/Test-ImplementationPreflight.ps1" -ProjectRoot $p|Out-Null;$text=Get-Content -Raw (Join-Path $p '.studio/DESIGN_SYSTEM.md');$text=$text -replace '"columnModel":\s*"12-column"','"columnModel":""';Set-Content (Join-Path $p '.studio/DESIGN_SYSTEM.md') $text;try{& "$root/scripts/Test-ImplementationPreflight.ps1" -ProjectRoot $p;throw 'Expected grid failure'}catch{if($_.Exception.Message -match 'Expected grid'){throw};if($_.Exception.Message -notmatch 'blocked'){throw}}}finally{Remove-Item $work -Recurse -Force};Write-Host 'Frontend-rule typed fixture passed; browser runtime unverified.'
