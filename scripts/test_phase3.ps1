[CmdletBinding()]param();$ErrorActionPreference='Stop';$root=(Resolve-Path (Join-Path $PSScriptRoot '..')).Path;. (Join-Path $root 'tests/helpers/TypedStudioFixture.ps1');$work=Join-Path ([IO.Path]::GetTempPath()) ('phase3-'+[guid]::NewGuid().ToString('N'));New-Item -ItemType Directory $work|Out-Null
try{$release=New-TypedStudioFixture $work 'release' -Surfaces @('frontend-visible','release') -Approved;$p=& "$root/scripts/Resolve-SpecialistPlan.ps1" -ProjectRoot $release;if($p.selected -notcontains 'release-engineer' -or $p.selected -contains 'operations-engineer'){throw 'Release routing incorrect'};$ops=New-TypedStudioFixture $work 'ops' -Surfaces @('backend-api','database','operations') -Approved;$p=& "$root/scripts/Resolve-SpecialistPlan.ps1" -ProjectRoot $ops;if($p.selected -notcontains 'operations-engineer'){throw 'Operations routing missing'}}finally{Remove-Item $work -Recurse -Force};Write-Host 'Phase 3 typed routing and deterministic-library boundaries passed; host runtime unverified.'
