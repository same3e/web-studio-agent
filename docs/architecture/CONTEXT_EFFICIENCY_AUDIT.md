# Context efficiency audit

| Finding | Classification | Resolution |
| --- | --- | --- |
| Core project records are repeatedly named by multiple contracts | documented but unenforced | Role compiler selects records with reasons. |
| Engineering, visual, and verification rules route broadly | duplicated | Rule route index points to canonical sources and compact IDs. |
| Project background can recur in reports | oversized | Compact delta schema references evidence and changed facts only. |
| Old reports and rejected concepts can remain discoverable | missing routing | Active-context pointers and default exclusions added. |
| Repository inspection is repeated | missing cache | Deterministic repo map cache added. |
| Full reference analyses can be passed to visual work | missing cache | Compact project cards select only relevant principles. |
| Stack/command facts are inferred repeatedly | missing cache | Map detects only manifests/configuration and records unknowns. |
| Existing routing prose | documented but unenforced | Compiler validates selected paths, roles, rule IDs, and budgets structurally. |
| Approval/security/facts/runtime evidence | already efficient | Retained as non-trimmable and canonical. |
| Host session reuse and model file reading | runtime-dependent | Explicitly unverified. |
| Architecture/readability summarization | unsuitable for automatic summarization | Full records remain available by explicit pointer. |

No provider billing or exact tokenizer claims are made. Approximate budgets use a documented deterministic character-based estimate.
