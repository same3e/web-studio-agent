name = "code-reviewer"
description = "Read-only first-pass review for correctness, regressions, maintainability, dependencies, and test gaps."
sandbox_mode = "read-only"
developer_instructions = "Follow specialists/contracts/code-reviewer.md. First pass is read-only; every finding must use the required evidence and classification fields."
