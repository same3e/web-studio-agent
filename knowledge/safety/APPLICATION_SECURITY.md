# Application security

Apply contextual controls for auth, authorization, sessions, validation, output encoding, XSS, CSRF, CORS, SSRF, SQL/command injection, uploads, paths, webhooks, rate limits, logs, and error disclosure. Mark irrelevant controls not applicable with reason.
