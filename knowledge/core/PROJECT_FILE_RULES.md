# PROJECT_FILE_RULES

## Purpose

Create only missing `.studio/` files and safely merge existing documents. Client-specific material never belongs in global knowledge, skills, or public packaging.

## Ownership and routing

- Owner: product-studio and add-reference.
- Load when the knowledge index route identifies knowledge/core/PROJECT_FILE_RULES.md for the current task, state, product type, or active risk.
- Do not load for unrelated read-only work; project-local decisions always override this global methodology.

## Applied rule

Create only missing `.studio/` files and safely merge existing documents. Client-specific material never belongs in global knowledge, skills, or public packaging.

## Project record

Record the resulting decision, evidence, assumption, or validation result in the relevant .studio/ source-of-truth document; do not store client data here.
