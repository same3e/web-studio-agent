# ACCESSIBILITY

## Purpose

Use semantic structure, keyboard operation, visible focus, labels, contrast, target sizes, reduced motion, and meaningful error announcements. Validate relevant behavior, not only code style.

## Ownership and routing

- Owner: product-studio.
- Load when the knowledge index route identifies knowledge/ux/ACCESSIBILITY.md for the current task, state, product type, or active risk.
- Do not load for unrelated read-only work; project-local decisions always override this global methodology.

## Applied rule

Use semantic structure, keyboard operation, visible focus, labels, contrast, target sizes, reduced motion, and meaningful error announcements. Validate relevant behavior, not only code style.

## Project record

Record the resulting decision, evidence, assumption, or validation result in the relevant .studio/ source-of-truth document; do not store client data here.
