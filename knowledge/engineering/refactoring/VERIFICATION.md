# Refactor verification

Preserve behavior through executed evidence, not confidence. Prefer incremental migration, stable module boundaries, dependency inversion where it lowers coupling, type improvements, duplicate/dead-code removal only with coverage, and an explicit rollback. Escalate public API, schema, copy, or product changes.
