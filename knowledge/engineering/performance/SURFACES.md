# Performance surfaces

Audit rendering, JavaScript/bundles, images, fonts, interaction responsiveness, serverless cold starts, backend latency, database queries, caches, queues/jobs, and external providers only when relevant. Assign remediation to the owning layer and recheck after the change.
