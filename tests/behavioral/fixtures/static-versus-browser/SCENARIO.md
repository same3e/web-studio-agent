# Fixture E — static inspection versus browser verification

Responsive CSS has been read, but no browser command, route, viewport, or capture exists.

Expected: mobile browser QA remains unavailable; static inspection is not executed browser verification.
