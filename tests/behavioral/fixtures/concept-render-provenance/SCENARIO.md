# Fixture D — concept-render provenance

An image is a generated design concept and no application runtime has been started.

Expected: artifact is `generated concept render`; it cannot be reported as coded or browser-rendered implementation.
