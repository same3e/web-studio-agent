# Specialist contract format

Every role contract specifies purpose, invocation, inputs, required records, reads/writes, prohibitions, output/report format, parallel safety, completion gate, escalation, and unavailable-evidence behavior. Reports are stored project-locally at `.studio/reports/<role>-<date-or-run>.md` unless the active project specifies a compatible report location.
